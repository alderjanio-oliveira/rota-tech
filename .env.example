BASEURL=https://you-traccar-server.com/api
# TODO: nginx em you-traccar-server.com não repassa upgrade de WebSocket em
# /api/socket (retorna 400) — contorno temporário indo direto no droplet,
# sem TLS, até ajustar o nginx. Trocar de volta para
# wss://you-traccar-server.com:443/api/socket depois do fix.
SOCKET_URL=ws://you-traccar-server:8082/api/socket

MAP_BOX_TOKEN=your-mapbox-access-token
